Readme: 

total lines of the created or modified code : 3472

1 install postgresql(database)

2 install nodejs server

3 install angular server

4 install elasticsearch

5 download the source code

6 cd to backend file and run
	1. npm install express
	2. npm install pg
	3. npm install pg-format
	4. npm install moment --save
	5. npm install elasticsearch

7 cd to frontend file and run
	1. npm install d3 --save
	2. npm install @agm/core --save
	3. npm install @types/googlemaps --save-dev 

8 start es server

9 cd to yelp file and run ChicagpSocialHub-Yelp

10 start postgresql server

11 cd to divvy file and run
	1. if divvy_stations_status and divvy_stations_logs tables don't exist, run createTablesInDatabase.py first

12 cd to backend file and start nodejs server

13 cd to divvy file and use anaconda prompt to run double-run.py script(collect data for divvy_stations_status and divvy_stations_logs tables)

14 cd to frontend file and start angular server, the default port number is 4200.